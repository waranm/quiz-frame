export default function Home() {
  return (
    <main>
      <h1>Quiz Frame (Farcaster)</h1>
      <p>Deploy is working! The frame lives at <code>/api</code>. Paste your deployed <code>/api</code> URL into a Farcaster cast to render the quiz.</p>
    </main>
  );
}
